file(GLOB_RECURSE HEADER_FILES ${CMAKE_CURRENT_LIST_DIR}/*.h)

set_target_properties(${LIBRARY_OBJLIB} ${LIBRARY_SHARED} ${LIBRARY_STATIC}
                      PROPERTIES PUBLIC_HEADER "${HEADER_FILES}")
